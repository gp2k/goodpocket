# Nixpacks/Railpack build config for Railway
# Ensures Python is detected and start command is correct when Root Directory = backend

[phases.setup]
nixPkgs = ["python312"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
